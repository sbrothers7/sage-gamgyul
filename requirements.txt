# 감귤 병해 AI 일반화 성능 분석 — 필요한 패키지
#
# 설치 방법 (Windows):
#   setup_windows.bat 을 더블클릭하면 자동으로 설치됩니다.
#
# 직접 설치하려면:
#   pip install -r requirements.txt
#
# ※ GPU(NVIDIA 그래픽카드)가 있다면 torch를 먼저 아래 명령으로 설치하세요.
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.0
torchvision>=0.15
timm>=0.9
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pillow>=10.0
opencv-python>=4.8
matplotlib>=3.7
PyYAML>=6.0
